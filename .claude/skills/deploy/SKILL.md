---
name: deploy
description: Build and deploy the app to Vercel production
---

Build the project and deploy to Vercel production.

1. Run `npm run build` to verify the build succeeds
2. If the build fails, fix the errors first
3. Deploy with `npx -y vercel --prod --yes`
4. Report the production URL when done
